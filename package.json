{
   "name": "stocks_portfolio",
   "version": "1.0.0",
   "description": "",
   "main": "index.js",
   "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "dev": "npm start --prefix frontend & npm run dev --prefix backend",
      "build": "npm run build --prefix frontend && npm run build --prefix backend",
      "build_start": "npm run build --prefix frontend && npm run dev --prefix backend",
      "build_docker": "npm run build --prefix frontend && npm run build --prefix backend && docker build  -t stocks_portfolio .",
      "docker_run": "docker run  --rm -p 127.0.0.1:5000:5000/tcp stocks_portfolio",
      "start": "npm start --prefix backend"
   },
   "repository": {
      "type": "git",
      "url": "git+https://github.com/bobse/stocks_portfolio.git"
   },
   "author": "Roberto Seba",
   "license": "ISC",
   "bugs": {
      "url": "https://github.com/bobse/stocks_portfolio/issues"
   },
   "homepage": "https://github.com/bobse/stocks_portfolio#readme"
}
